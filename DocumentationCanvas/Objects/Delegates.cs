using Grasshopper.GUI.Canvas;

namespace DocumentationCanvas.Objects
{
    public delegate void PostPaintEventHandler(GH_Canvas canvas);

    public delegate void PostValidityChangedEventHandler();
}
